# Решение проблемы CORS для локальной разработки

## ✅ Что уже настроено:

1. **CORS_ALLOW_ALL_ORIGINS = True** - разрешает все источники
2. **CORS_ALLOWED_ORIGIN_REGEXES** - разрешает любые localhost порты
3. **Создан .env.local** с `VITE_API_BASE_URL=http://localhost:8000`

## 🔧 Что нужно сделать:

### 1. Убедитесь, что бэкенд запущен:
```bash
cd Backend
python3 manage.py runserver 0.0.0.0:8000
```

### 2. Перезапустите dev-сервер фронтенда:
```bash
cd Frontend
# Остановите текущий сервер (Ctrl+C)
npm run dev
# или
bun dev
```

**Важно:** Vite загружает переменные из `.env.local` только при старте, поэтому нужен перезапуск!

### 3. Проверьте, что используется правильный URL:
Откройте консоль браузера (F12) и проверьте:
- В Network tab должны быть запросы на `http://localhost:8000`
- Не на ngrok URL!

### 4. Если всё ещё не работает:

Проверьте порядок middleware в `Backend/config/settings.py`:
```python
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'corsheaders.middleware.CorsMiddleware',  # Должен быть ПЕРЕД CommonMiddleware!
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    # ...
]
```

### 5. Проверьте, что бэкенд доступен:
Откройте в браузере: `http://localhost:8000/api/exchange-rates/`
Должен вернуться JSON с курсами.

## 🐛 Отладка:

Если ошибка CORS всё ещё есть, проверьте в консоли браузера:
- Какой URL используется в запросах?
- Какая точная ошибка CORS?
- Есть ли ошибки в консоли бэкенда?

## 📝 Файлы:

- `.env` - для Netlify (ngrok URL)
- `.env.local` - для локальной разработки (localhost:8000) - **приоритет!**

