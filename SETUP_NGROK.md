# Настройка для работы с ngrok

## ✅ Бэкенд настроен

Ваш ngrok URL: `https://94bac74dc099.ngrok-free.app`

Настройки уже обновлены:
- ✅ Добавлен в `ALLOWED_HOSTS`
- ✅ Добавлен в `CORS_ALLOWED_ORIGINS`
- ✅ `CORS_ALLOW_ALL_ORIGINS = True` (разрешает все источники)

## 📝 Что нужно сделать на фронтенде:

1. **Создайте файл `.env` в папке `Frontend`**:
   ```
   VITE_API_BASE_URL=https://94bac74dc099.ngrok-free.app
   ```

2. **Пересоберите проект**:
   ```bash
   cd Frontend
   npm run build
   ```

3. **Задеплойте на Netlify** (или обновите существующий деплой)

## 🚀 Запуск:

1. **Запустите бэкенд**:
   ```bash
   cd Backend
   python3 manage.py runserver 0.0.0.0:8000
   ```

2. **Запустите ngrok** (в другом терминале):
   ```bash
   ngrok http 8000
   ```

3. **Проверьте**, что ngrok показывает тот же URL: `https://94bac74dc099.ngrok-free.app`

## ⚠️ Важно:

- **Ngrok URL меняется** при каждом перезапуске (в бесплатной версии)
- Если URL изменится, обновите:
  - `ALLOWED_HOSTS` в `Backend/config/settings.py`
  - `CORS_ALLOWED_ORIGINS` в `Backend/config/settings.py`
  - `.env` файл на фронтенде
  - Пересоберите и задеплойте фронтенд

- **Для постоянного URL** используйте платную версию ngrok или зарегистрируйте бесплатный аккаунт с фиксированным доменом

## 🧪 Проверка:

Откройте в браузере:
- `https://94bac74dc099.ngrok-free.app/admin/` - должна открыться админка Django
- `https://94bac74dc099.ngrok-free.app/api/exchange-rates/` - должен вернуться JSON с курсами

Если все работает, значит бэкенд доступен из интернета! 🎉

